# AXERP (ERPNext v16) — Production Compose
# Domain:   erp.tspgusa.com
# Database: axerp-mariadb (MariaDB 10.6) — dedicated container, arm64
#           ERPNext requires MariaDB; PostgreSQL has thousands of incompatible
#           raw SQL queries (sum(unknown), DATE_SUB, etc.) across the codebase.
# Port:     127.0.0.1:8082:8080 (proxied by openproject-nginx)
# Platform: linux/arm64 (Graviton2 EC2)
#
# Database layout on this host:
#   openproject-postgres  → OpenProject + Nextcloud  (PostgreSQL 16)
#   axerp-mariadb         → ERPNext only              (MariaDB 10.6)
#
# Image tag convention: axerp:<ERPNEXT_VERSION>-axerp.<PATCH>
# Current: 010438486646.dkr.ecr.us-east-1.amazonaws.com/axerp:v16.26.2-axerp.5
# Images stored in AWS ECR Private (us-east-1). EC2 pulls via IAM role (no credentials needed).
# To upgrade upstream: bump tag, pull + rebuild on EC2, redeploy.
#
# Apps: frappe → erpnext → crm → hrms → insights → wiki → blog
#
# Deploy (EC2 via SSM):
#   bash scripts/deploy.sh
#
# Monitor first-run site creation:
#   docker logs axerp-create-site -f    # exits 0 on success (~5–10 min)
#
# After every deploy, migrate runs automatically via the migrate service.

x-axerp-image: &axerp_image
  image: 010438486646.dkr.ecr.us-east-1.amazonaws.com/axerp:v16.26.2-axerp.5
  platform: linux/arm64
  restart: unless-stopped
  networks:
    - openproject_default
  volumes:
    - /data/axerp/sites:/home/frappe/frappe-bench/sites
    - /data/axerp/logs:/home/frappe/frappe-bench/logs
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "3"

services:

  # ── MariaDB — dedicated to ERPNext ───────────────────────────────────────
  axerp-mariadb:
    image: mariadb:10.6
    platform: linux/arm64
    container_name: axerp-mariadb
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${AXERP_DB_ROOT_PASSWORD}
      MYSQL_ROOT_HOST: "%"
    volumes:
      - /data/axerp/mariadb:/var/lib/mysql
    networks:
      - openproject_default
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 12
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --skip-character-set-client-handshake
      - --skip-innodb-read-only-compressed
      - --bind-address=0.0.0.0

  # ── Frappe services ───────────────────────────────────────────────────────
  backend:
    <<: *axerp_image
    container_name: axerp-backend
    deploy:
      resources:
        limits:
          memory: 2g
    depends_on:
      configurator:
        condition: service_completed_successfully
      migrate:
        condition: service_completed_successfully

  configurator:
    <<: *axerp_image
    container_name: axerp-configurator
    restart: "no"
    entrypoint:
      - bash
      - -c
    command:
      - >
        ls -1 apps > sites/apps.txt;
        [ -f sites/common_site_config.json ] || echo '{}' > sites/common_site_config.json;
        bench set-config -g db_host $$DB_HOST;
        bench set-config -gp db_port $$DB_PORT;
        bench set-config -g redis_cache "redis://$$REDIS_CACHE";
        bench set-config -g redis_queue "redis://$$REDIS_QUEUE";
        bench set-config -g redis_socketio "redis://$$REDIS_QUEUE";
        bench set-config -gp socketio_port $$SOCKETIO_PORT;
    environment:
      DB_HOST: axerp-mariadb
      DB_PORT: "3306"
      REDIS_CACHE: axerp-redis-cache:6379
      REDIS_QUEUE: axerp-redis-queue:6379
      SOCKETIO_PORT: "9000"
    depends_on:
      axerp-mariadb:
        condition: service_healthy
      axerp-redis-cache:
        condition: service_started
      axerp-redis-queue:
        condition: service_started

  # ── First-run site creation (skipped if site already exists) ─────────────
  create-site:
    <<: *axerp_image
    container_name: axerp-create-site
    restart: "no"
    entrypoint:
      - bash
      - -c
    command:
      - >
        wait-for-it -t 120 axerp-mariadb:3306;
        wait-for-it -t 120 axerp-redis-cache:6379;
        wait-for-it -t 120 axerp-redis-queue:6379;
        export start=`date +%s`;
        until [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".db_host // empty"` ]] && \
          [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".redis_cache // empty"` ]] && \
          [[ -n `grep -hs ^ sites/common_site_config.json | jq -r ".redis_queue // empty"` ]];
        do
          echo "Waiting for sites/common_site_config.json...";
          sleep 5;
          if (( `date +%s`-start > 120 )); then
            echo "ERROR: sites/common_site_config.json not ready after 120s";
            exit 1;
          fi;
        done;
        if [ -f "sites/$$SITE_NAME/site_config.json" ]; then
          echo "Site $$SITE_NAME already exists — skipping creation.";
          exit 0;
        fi;
        echo "Creating site $$SITE_NAME on MariaDB...";
        bench new-site \
          --mariadb-root-password $$DB_ROOT_PASSWORD \
          --mariadb-user-host-login-scope='%' \
          --admin-password $$SITE_ADMIN_PASSWORD \
          --install-app erpnext \
          --set-default $$SITE_NAME;
        echo "Installing hrms...";
        bench --site $$SITE_NAME install-app hrms;
        echo "Installing crm...";
        bench --site $$SITE_NAME install-app crm;
        echo "Installing insights...";
        bench --site $$SITE_NAME install-app insights;
        echo "Installing wiki...";
        bench --site $$SITE_NAME install-app wiki;
        echo "Installing blog...";
        bench --site $$SITE_NAME install-app blog;
        echo "Site creation complete.";
    environment:
      SITE_NAME: ${AXERP_SITE_NAME:-erp.tspgusa.com}
      DB_ROOT_PASSWORD: ${AXERP_DB_ROOT_PASSWORD}
      SITE_ADMIN_PASSWORD: ${AXERP_ADMIN_PASSWORD}
    depends_on:
      configurator:
        condition: service_completed_successfully
      axerp-mariadb:
        condition: service_healthy

  # ── DB migration — runs on every deploy, catches new doctypes/fields ──────
  migrate:
    <<: *axerp_image
    container_name: axerp-migrate
    restart: "no"
    entrypoint:
      - bash
      - -c
    command:
      - >
        wait-for-it -t 120 axerp-mariadb:3306;
        wait-for-it -t 120 axerp-redis-cache:6379;
        if [ ! -f "sites/$$SITE_NAME/site_config.json" ]; then
          echo "Site $$SITE_NAME does not exist yet — skipping migrate (create-site will handle it).";
          exit 0;
        fi;
        echo "=== [1/3] bench migrate — apply DB schema + patches ===";
        bench --site $$SITE_NAME migrate;
        echo "=== [2/3] bench build --production — rebuild all JS/CSS bundles ===";
        bench build --production;
        echo "=== [3/3] Clear site caches ===";
        bench --site $$SITE_NAME clear-cache;
        bench --site $$SITE_NAME clear-website-cache;
        echo "Done.";
    environment:
      SITE_NAME: ${AXERP_SITE_NAME:-erp.tspgusa.com}
    depends_on:
      configurator:
        condition: service_completed_successfully
      create-site:
        condition: service_completed_successfully
      axerp-mariadb:
        condition: service_healthy

  frontend:
    <<: *axerp_image
    container_name: axerp-frontend
    restart: on-failure
    command:
      - nginx-entrypoint.sh
    environment:
      BACKEND: axerp-backend:8000
      FRAPPE_SITE_NAME_HEADER: ${AXERP_SITE_NAME:-erp.tspgusa.com}
      SOCKETIO: axerp-websocket:9000
      UPSTREAM_REAL_IP_ADDRESS: 127.0.0.1
      UPSTREAM_REAL_IP_HEADER: X-Forwarded-For
      UPSTREAM_REAL_IP_RECURSIVE: "off"
      PROXY_READ_TIMEOUT: 120
      CLIENT_MAX_BODY_SIZE: 100m
    ports:
      - "127.0.0.1:8082:8080"
    volumes:
      - /data/axerp/sites:/home/frappe/frappe-bench/sites
      - /data/axerp/logs:/home/frappe/frappe-bench/logs
      # Persist patched nginx config — fixes broken frappe/public/css/fonts/inter relative path
      - /data/axerp/sites/frappe_nginx.conf:/etc/nginx/conf.d/frappe.conf:ro
    depends_on:
      backend:
        condition: service_started
      websocket:
        condition: service_started

  websocket:
    <<: *axerp_image
    container_name: axerp-websocket
    command:
      - node
      - /home/frappe/frappe-bench/apps/frappe/socketio.js
    environment:
      FRAPPE_REDIS_CACHE: redis://axerp-redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://axerp-redis-queue:6379
    depends_on:
      configurator:
        condition: service_completed_successfully

  queue-long:
    <<: *axerp_image
    container_name: axerp-queue-long
    command:
      - bench
      - worker
      - --queue
      - long,default,short
    environment:
      FRAPPE_REDIS_CACHE: redis://axerp-redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://axerp-redis-queue:6379
    deploy:
      resources:
        limits:
          memory: 1g
    depends_on:
      configurator:
        condition: service_completed_successfully

  queue-short:
    <<: *axerp_image
    container_name: axerp-queue-short
    command:
      - bench
      - worker
      - --queue
      - short,default
    environment:
      FRAPPE_REDIS_CACHE: redis://axerp-redis-cache:6379
      FRAPPE_REDIS_QUEUE: redis://axerp-redis-queue:6379
    deploy:
      resources:
        limits:
          memory: 512m
    depends_on:
      configurator:
        condition: service_completed_successfully

  scheduler:
    <<: *axerp_image
    container_name: axerp-scheduler
    command:
      - bench
      - schedule
    depends_on:
      configurator:
        condition: service_completed_successfully

  axerp-redis-cache:
    image: redis:6.2-alpine
    platform: linux/arm64
    container_name: axerp-redis-cache
    restart: unless-stopped
    command: redis-server --save "" --appendonly no
    networks:
      - openproject_default

  axerp-redis-queue:
    image: redis:6.2-alpine
    platform: linux/arm64
    container_name: axerp-redis-queue
    restart: unless-stopped
    volumes:
      - /data/axerp/redis-queue:/data
    networks:
      - openproject_default

networks:
  openproject_default:
    external: true
